# chronorift-playable
Isolated public GitHub Pages runtime for ChronoRift playable builds. Static HTML/CSS/JS only; no secrets, dependencies, Actions, or cross-repo access. Built as a no-cost, disposable public layer for iPhone testing, SelfDebug reports, safe iteration, and future scalability when ready. Keeps ancestor repos protected and receives safe game updates. v1
